# Bookmark Manager

## Database Setup
* Connect to ```psql```.
* Create the ```bookmark_manager``` and ```bookmark_manager_test``` databases.
* Connect to the database using ```\c``` command.
* Run the query in each database ```01_create_bookmarks_table.sql``` in ```db -> migrations``` folder within this directory.
